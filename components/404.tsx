import styled from "styled-components";

const NotFound = (): JSX.Element => {
  return <ErrorContainer>Parece que você se perdeu</ErrorContainer>;
};

const ErrorContainer = styled.div``;

export default NotFound;
